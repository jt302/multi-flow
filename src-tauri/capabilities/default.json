{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for main and log windows",
  "windows": ["main", "log-panel"],
  "permissions": [
    "core:default",
    "core:app:allow-set-app-theme",
    "opener:default"
  ]
}
